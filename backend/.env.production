# 生产环境配置
# 这些配置会覆盖 .env 中的默认值

# 环境标识
NODE_ENV=production

# 生产环境数据库
DATABASE_URL=postgresql://safereach:SafeReach123!@1.12.60.17:5432/safereach

# 生产环境 CORS 配置（允许移动应用访问）
CORS_ORIGIN=*

# 生产环境日志级别
LOG_LEVEL=warn

# 生产环境 JWT 配置
JWT_EXPIRES_IN=7d

# 生产环境密码加密轮数（更高的安全性）
BCRYPT_ROUNDS=15
