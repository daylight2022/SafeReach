# 开发环境配置
# 这些配置会覆盖 .env 中的默认值

# 环境标识
NODE_ENV=development

# 开发环境数据库 - 临时使用远程数据库进行测试
DATABASE_URL=postgresql://safereach:SafeReach123!@localhost:5432/safereach

# 开发环境 CORS 配置
CORS_ORIGIN=*
# CORS_ORIGIN=http://localhost:3000,http://localhost:8081,http://localhost:19006

# 开发环境日志级别
LOG_LEVEL=debug

# 开发环境 JWT 过期时间（更长的过期时间便于开发）
JWT_EXPIRES_IN=30d
